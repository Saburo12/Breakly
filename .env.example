# Anthropic Claude API Configuration
ANTHROPIC_API_KEY=your_api_key_here

# Database Configuration (optional, will use mock mode if not set)
DATABASE_URL=postgresql://postgres:password@localhost:5432/lovable_clone

# Environment
NODE_ENV=development

# Mock Claude Service (set to true for demo mode without API key)
MOCK_CLAUDE=false

# Backend Server Port
BACKEND_PORT=3001

# Frontend Server Port
FRONTEND_PORT=3000
